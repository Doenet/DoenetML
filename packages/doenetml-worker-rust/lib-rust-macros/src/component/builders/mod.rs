//! This module houses all the builders for the various structs associated with a component.
//! They are the `Component` struct, and the `Actions`, `Attributes`, and `Props` enums.

mod actions;
mod attributes;
mod component;
mod general;
mod props;
