# Components without a /reference/<slug> docs page.
#
# Components listed here will not produce a "Reference page" link in editor
# context help, and the docs-coverage CI check will warn (not fail) for them.
# Components NOT in this list AND not having a docs page will fail CI.
#
# To remove an entry: write a docs page (or set componentDocs.docsSlug on the
# class to point to an existing page) and delete the line here.
#
# Lines starting with `#` and blank lines are ignored.

annotation
annotations
attractToConstraint
cascade
cascadeMessage
cellBlock
clampFunction
codeEditor
column
constrainToInterior
dataFrame
displayDoenetML
electronConfiguration
extractMathOperator
feedbackDefinition
givenAnswer
ion
ionicCompound
latex
lcm
markers
note
orbitalDiagram
pegboard
periodicSet
pluralize
regionHalfPlane
solveEquations
stickyGroup
subsetOfRealsInput
summaryStatistics
tagc
tage
topic
variantControl
wrapFunctionPeriodic
