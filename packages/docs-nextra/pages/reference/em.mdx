import { DoenetViewer, DoenetEditor, DoenetExample } from "../../components"

import { AttrDisplay, PropDisplay } from "../../components"


# ` <em>{:dn}`

## Attributes and Properties 
 <AttrDisplay name='em'/> 
 <PropDisplay name='em'/>


The ` <em>{:dn}` component is a paragraph markup function which renders the enclosed text in emphasized, or italicized, font.
