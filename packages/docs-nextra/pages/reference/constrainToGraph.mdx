import { DoenetViewer, DoenetEditor, DoenetExample } from "../../components"

import { ComponentDisplay, AttrPropDisplay } from "../../components"


# `<constrainToGraph>{:dn}`

<ComponentDisplay name='constrainToGraph'/>

`<constrainToGraph>{:dn}` is a [Graphical](../concepts/essentialConcepts#component-types)
component to keep constrained `<point>{:dn}` 
components within the confines of the graphing window.

<AttrPropDisplay name='constrainToGraph'/>

## Examples

### Example: Constrain a `<point>{:dn}` to a `<graph>{:dn}`


```doenet-editor-horiz
<p>Try dragging the point off the graph</p>
<graph size="small">
  <shortDescription>A graph with a point constrained to the viewing window</shortDescription>
  <point name="p">
    <constrainToGraph/>
    <label>$p.coords</label>
  </point>
</graph>
```



The `<point>{:dn}` is constrained to the current viewing window of the `<graph>{:dn}`.



## Attribute Examples

### Attribute Example: buffer


```doenet-editor-horiz
<p>Try dragging the point off the graph</p>
<graph size="small">
  <shortDescription>A graph with a point constrained to the viewing window with a buffer</shortDescription>
  <point name="p">
    <constrainToGraph buffer="0.2"/>
    <label>$p.coords</label>
  </point>
</graph>
```



The `<point>{:dn}` is constrained to the current viewing window of 
the `<graph>{:dn}` with a specified `buffer` of $20 \%$ of the graph.


