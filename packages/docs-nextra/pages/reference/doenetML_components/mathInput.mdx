# `<mathInput>`

### What it does
`<mathInput/>` renders a user-input field that stores mathematical content. When copied, a named `<mathInput/>` can be used in computations in the same manner as a `<math>` component.