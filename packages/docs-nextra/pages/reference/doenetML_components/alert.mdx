import { DoenetViewer, DoenetEditor, DoenetExample, AttrDisplay, PropDisplay } from "../../../components"

# `<alert>`

## What it does
`<alert>` is a [**Paragraph Markup**](../componentClasses) component which renders the enclosed text in bold font.

## Attributes and Properties
The `<alert>` component has no attributes.

<PropDisplay name='alert'/>

## Examples

### Example: Basic use
```doenet-editor-horiz
<p>Take the <alert>right</alert> turn.</p>
```